This editor allows for multiple people to collaborate on an HTML page.
The goal is to get it to work for HTML, CSS, and Javascript.  

To get up and running:

1. npm install socket.io
2. npm install url
3. node server.js 
4. localhost will be listening on port 8000